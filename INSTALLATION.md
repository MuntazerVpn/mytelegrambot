# 📦 دليل التثبيت التفصيلي - Installation Guide

## 🌟 المتطلبات الأساسية

### 1. Node.js
يجب تثبيت Node.js الإصدار 18 أو أحدث.

**للتحقق من الإصدار:**
```bash
node --version
```

**للتثبيت:**
- **Windows/Mac**: قم بتحميل المثبت من [nodejs.org](https://nodejs.org/)
- **Linux (Ubuntu/Debian)**:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 2. Git (اختياري)
لاستنساخ المشروع من GitHub.

```bash
git --version
```

## 🚀 خطوات التثبيت

### الخطوة 1: الحصول على Bot Token

1. افتح Telegram وابحث عن [@BotFather](https://t.me/BotFather)
2. أرسل الأمر `/newbot`
3. اتبع التعليمات لإنشاء البوت
4. احفظ الـ Token الذي سيرسله لك BotFather

**مثال على Token:**
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### الخطوة 2: تحميل المشروع

**الطريقة 1: باستخدام Git**
```bash
git clone <repository-url>
cd telegram-downloader-bot
```

**الطريقة 2: تحميل ZIP**
1. قم بتحميل ملف ZIP من GitHub
2. فك الضغط
3. افتح Terminal/CMD في مجلد المشروع

### الخطوة 3: تثبيت المكتبات

```bash
npm install
```

**المكتبات المثبتة:**
- `node-telegram-bot-api` - للتعامل مع Telegram API
- `axios` - لطلبات HTTP
- `ytdl-core` - لتحميل من YouTube
- `node-cron` - للمهام المجدولة
- `better-sqlite3` - قاعدة البيانات

### الخطوة 4: إعداد ملف البيئة

1. **انسخ ملف المثال:**
```bash
cp .env.example .env
```

2. **افتح ملف `.env` وعدّل القيم:**

```env
# ضع Token البوت هنا
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ضع معرفات المشرفين (افصل بفاصلة)
# للحصول على معرفك، أرسل رسالة لـ @userinfobot
ADMIN_IDS=123456789,987654321

# مسار قاعدة البيانات (اتركه كما هو)
DB_PATH=./data/bot.db

# إعدادات Rate Limiting
RATE_LIMIT_WINDOW=60000          # نافذة زمنية بالميلي ثانية (60 ثانية)
RATE_LIMIT_MAX_REQUESTS=10       # أقصى عدد طلبات في النافذة

# إعدادات Anti-Spam
SPAM_THRESHOLD=5                 # عدد الرسائل المتتالية للاعتبار سبام
SPAM_BAN_DURATION=3600000        # مدة الحظر المؤقت (ساعة واحدة)

# إعدادات التحميل
MAX_FILE_SIZE=2000000000         # الحد الأقصى لحجم الملف (2GB)
TEMP_DIR=./temp                  # مجلد الملفات المؤقتة
AUTO_CLEANUP_HOURS=24            # حذف الملفات بعد (24 ساعة)

# الاشتراك الإجباري (اختياري - اتركه فارغاً إذا لم تحتاجه)
FORCE_CHANNEL_ID=                # معرف القناة (مثال: -1001234567890)
FORCE_CHANNEL_USERNAME=          # اسم القناة (بدون @)
```

### الخطوة 5: الحصول على معرف المستخدم (User ID)

لإضافة نفسك كمشرف:

1. افتح Telegram
2. ابحث عن [@userinfobot](https://t.me/userinfobot)
3. أرسل له أي رسالة
4. سيرسل لك معرفك (User ID)
5. أضف المعرف في `ADMIN_IDS` في ملف `.env`

### الخطوة 6: إعداد الاشتراك الإجباري (اختياري)

إذا أردت إجبار المستخدمين على الاشتراك في قناة:

1. **أنشئ قناة في Telegram**
2. **أضف البوت كمشرف في القناة**
3. **احصل على معرف القناة:**
   - أرسل رسالة في القناة
   - أعد توجيه الرسالة إلى [@userinfobot](https://t.me/userinfobot)
   - سيرسل لك معرف القناة (مثال: -1001234567890)

4. **عدّل ملف `.env`:**
```env
FORCE_CHANNEL_ID=-1001234567890
FORCE_CHANNEL_USERNAME=your_channel
```

### الخطوة 7: تشغيل البوت

**وضع الإنتاج:**
```bash
npm start
```

**وضع التطوير (مع إعادة التشغيل التلقائي):**
```bash
npm run dev
```

**يجب أن ترى:**
```
✅ Bot started successfully!
📊 Database initialized
🔄 Cleanup service running
👋 Waiting for messages...
```

## ✅ التحقق من التثبيت

1. **افتح Telegram**
2. **ابحث عن البوت** (الاسم الذي أعطيته للبوت في BotFather)
3. **أرسل `/start`**
4. **يجب أن يرد البوت** برسالة ترحيب وقائمة الأزرار

## 🔧 حل المشاكل الشائعة

### المشكلة: "BOT_TOKEN is not set"
**الحل:**
- تأكد من وجود ملف `.env` في المجلد الرئيسي
- تأكد من كتابة `BOT_TOKEN` بشكل صحيح
- تأكد من عدم وجود مسافات قبل أو بعد Token

### المشكلة: "Error: ETELEGRAM: 401 Unauthorized"
**الحل:**
- Token غير صحيح
- احصل على Token جديد من @BotFather

### المشكلة: "Cannot find module"
**الحل:**
```bash
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: البوت لا يستجيب
**الحل:**
- تأكد من تشغيل البوت (`npm start`)
- تحقق من اتصال الإنترنت
- راجع رسائل الخطأ في Terminal

### المشكلة: "EACCES: permission denied"
**الحل:**
```bash
# Linux/Mac
sudo chown -R $USER:$USER .
chmod -R 755 .

# أو استخدم
sudo npm install
```

### المشكلة: التحميل لا يعمل
**الحل:**
- تأكد من صحة الرابط
- بعض المنصات قد تحتاج إعدادات إضافية
- تحقق من مساحة القرص

## 🖥️ تشغيل البوت في الخلفية

### Linux/Mac - باستخدام PM2

1. **تثبيت PM2:**
```bash
npm install -g pm2
```

2. **تشغيل البوت:**
```bash
pm2 start src/index.js --name telegram-bot
```

3. **أوامر مفيدة:**
```bash
pm2 status              # حالة البوت
pm2 logs telegram-bot   # عرض السجلات
pm2 restart telegram-bot # إعادة التشغيل
pm2 stop telegram-bot   # إيقاف البوت
pm2 startup             # تشغيل تلقائي عند بدء النظام
```

### Windows - باستخدام pm2-windows-service

```bash
npm install -g pm2
npm install -g pm2-windows-service

pm2-service-install
pm2 start src/index.js --name telegram-bot
```

## 🐳 التشغيل باستخدام Docker (متقدم)

1. **أنشئ ملف `Dockerfile`:**
```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

CMD ["npm", "start"]
```

2. **أنشئ ملف `docker-compose.yml`:**
```yaml
version: '3.8'
services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./temp:/app/temp
```

3. **شغّل البوت:**
```bash
docker-compose up -d
```

## 📱 اختبار البوت

### اختبار الوظائف الأساسية:
1. ✅ `/start` - يجب أن يرد بالترحيب
2. ✅ `/help` - يجب أن يعرض المساعدة
3. ✅ `/download` - يجب أن يطلب رابط
4. ✅ أرسل رابط YouTube - يجب أن يعرض الجودات

### اختبار لوحة الإدارة:
1. ✅ `/admin` - يجب أن تفتح لوحة الإدارة
2. ✅ `/stats` - يجب أن تعرض الإحصائيات
3. ✅ اختبر الإذاعة
4. ✅ اختبر حظر/إلغاء حظر

## 🔄 التحديثات

للحصول على آخر التحديثات:

```bash
git pull origin main
npm install
npm start
```

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم "حل المشاكل الشائعة" أعلاه
2. تحقق من السجلات في Terminal
3. افتح Issue في GitHub
4. راسلنا على Telegram

---

🎉 **تهانينا! البوت جاهز للاستخدام**
