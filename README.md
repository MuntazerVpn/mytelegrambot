# 🤖 Telegram Downloader Bot - بوت تحميل تيليجرام الاحترافي

بوت تيليجرام احترافي لتحميل الفيديوهات والصوتيات من منصات متعددة مع لوحة إدارة متكاملة ودعم لغتين (العربية والإنجليزية).

## ✨ المميزات الرئيسية

### 📥 نظام التحميل
- ✅ دعم منصات متعددة (YouTube, Instagram, TikTok, Facebook, Twitter)
- ✅ عرض معلومات الملف قبل التحميل (العنوان، المدة، الحجم)
- ✅ اختيار الجودة المطلوبة
- ✅ خيار إرسال كملف أو رابط مباشر
- ✅ شريط تقدم التحميل
- ✅ سجل تحميلات لكل مستخدم

### 🎨 واجهة المستخدم
- ✅ أزرار Inline احترافية
- ✅ قوائم تفاعلية سهلة الاستخدام
- ✅ دعم لغتين (العربية والإنجليزية)
- ✅ تبديل اللغة بسهولة

### 👑 لوحة الإدارة الكاملة
- ✅ **الإحصائيات المفصلة**
  - إجمالي المستخدمين
  - المستخدمين النشطين اليوم
  - إجمالي التحميلات
  - التحميلات اليومية
  - عدد المحظورين

- ✅ **إدارة المستخدمين**
  - عرض المستخدمين النشطين
  - عرض المستخدمين المحظورين
  - حظر/إلغاء حظر المستخدمين
  - نظام أدوار متعدد (Owner, Admin, Moderator, User)

- ✅ **نظام الإذاعة**
  - إذاعة فورية لجميع المستخدمين
  - إذاعة مجدولة
  - تتبع حالة الإرسال

- ✅ **سجل العمليات**
  - تسجيل جميع الأحداث
  - تتبع نشاطات المستخدمين
  - تتبع نشاطات الإدارة

- ✅ **الاشتراك الإجباري**
  - إجبار المستخدمين على الاشتراك في قناة
  - التحقق التلقائي من الاشتراك

### 🛡️ نظام الحماية والأمان
- ✅ **Rate Limiting**
  - حد أقصى للطلبات في فترة زمنية محددة
  - منع إساءة الاستخدام

- ✅ **Anti-Spam**
  - كشف السبام التلقائي
  - حظر مؤقت للمستخدمين المخالفين
  - عتبة قابلة للتخصيص

- ✅ **التنظيف التلقائي**
  - حذف الملفات القديمة تلقائياً
  - توفير مساحة التخزين

## 🚀 التثبيت والإعداد

### المتطلبات
- Node.js 18 أو أحدث
- حساب Telegram Bot (احصل على Token من [@BotFather](https://t.me/BotFather))

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd telegram-downloader-bot
```

2. **تثبيت المكتبات**
```bash
npm install
```

3. **إعداد ملف البيئة**
```bash
cp .env.example .env
```

4. **تعديل ملف `.env`**
```env
# معلومات البوت
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321

# قاعدة البيانات
DB_PATH=./data/bot.db

# Rate Limiting
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=10

# Anti-Spam
SPAM_THRESHOLD=5
SPAM_BAN_DURATION=3600000

# إعدادات التحميل
MAX_FILE_SIZE=2000000000
TEMP_DIR=./temp
AUTO_CLEANUP_HOURS=24

# الاشتراك الإجباري (اختياري)
FORCE_CHANNEL_ID=-1001234567890
FORCE_CHANNEL_USERNAME=your_channel
```

5. **تشغيل البوت**
```bash
# وضع الإنتاج
npm start

# وضع التطوير (مع إعادة التشغيل التلقائي)
npm run dev
```

## 📖 دليل الاستخدام

### للمستخدمين

#### الأوامر الأساسية
- `/start` - بدء البوت والحصول على القائمة الرئيسية
- `/download` - بدء عملية تحميل جديدة
- `/history` - عرض سجل التحميلات
- `/settings` - إعدادات المستخدم
- `/help` - عرض المساعدة
- `/language` - تغيير اللغة

#### طريقة التحميل
1. أرسل الأمر `/download` أو اضغط على زر "⬇️ تحميل"
2. أرسل رابط الفيديو أو الصوت
3. اختر الجودة المطلوبة من القائمة
4. اختر إرسال كملف أو رابط مباشر
5. انتظر حتى يكتمل التحميل

### للمشرفين

#### الأوامر الإدارية
- `/admin` - فتح لوحة الإدارة
- `/stats` - عرض الإحصائيات
- `/broadcast` - إرسال إذاعة
- `/ban` - حظر مستخدم
- `/unban` - إلغاء حظر مستخدم

#### لوحة الإدارة
- **الإحصائيات**: عرض إحصائيات مفصلة عن البوت
- **المستخدمين**: إدارة المستخدمين والمحظورين
- **الإذاعة**: إرسال رسائل جماعية
- **السجلات**: عرض سجل العمليات
- **الأدوار**: تعيين أدوار للمستخدمين
- **الاشتراك الإجباري**: إعداد قناة إجبارية

## 🏗️ هيكل المشروع

```
telegram-downloader-bot/
├── src/
│   ├── config/
│   │   ├── config.js          # إعدادات البوت
│   │   └── languages.js       # ملفات اللغات
│   ├── database/
│   │   └── database.js        # إدارة قاعدة البيانات
│   ├── handlers/
│   │   ├── commandHandlers.js # معالجات الأوامر
│   │   ├── callbackHandlers.js # معالجات الأزرار
│   │   └── messageHandlers.js  # معالجات الرسائل
│   ├── services/
│   │   ├── downloader.js      # خدمة التحميل
│   │   └── keyboard.js        # خدمة الأزرار
│   ├── utils/
│   │   └── cleanup.js         # خدمة التنظيف
│   └── index.js               # نقطة البداية
├── data/                      # قاعدة البيانات
├── temp/                      # الملفات المؤقتة
├── .env                       # متغيرات البيئة
├── .env.example              # مثال لمتغيرات البيئة
├── package.json
└── README.md
```

## 🔧 التخصيص

### إضافة لغة جديدة
عدّل ملف `src/config/languages.js` وأضف اللغة الجديدة:

```javascript
export const languages = {
  ar: { /* النصوص العربية */ },
  en: { /* النصوص الإنجليزية */ },
  fr: { /* النصوص الفرنسية */ }
};
```

### تخصيص الأدوار والصلاحيات
عدّل ملف `src/config/config.js`:

```javascript
export const ROLES = {
  OWNER: 'owner',
  ADMIN: 'admin',
  MODERATOR: 'moderator',
  USER: 'user'
};

export const PERMISSIONS = {
  [ROLES.OWNER]: ['*'],
  [ROLES.ADMIN]: ['broadcast', 'stats', 'ban', 'unban'],
  // ...
};
```

### إضافة منصة تحميل جديدة
عدّل ملف `src/services/downloader.js` وأضف دعم المنصة الجديدة.

## 📊 قاعدة البيانات

يستخدم البوت SQLite مع الجداول التالية:
- `users` - معلومات المستخدمين
- `downloads` - سجل التحميلات
- `broadcasts` - سجل الإذاعات
- `activity_logs` - سجل العمليات
- `rate_limits` - تتبع Rate Limiting
- `spam_detection` - كشف السبام
- `settings` - إعدادات البوت

## 🔒 الأمان

- ✅ جميع البيانات الحساسة في ملف `.env`
- ✅ Rate Limiting لمنع إساءة الاستخدام
- ✅ Anti-Spam للحماية من السبام
- ✅ نظام صلاحيات متعدد المستويات
- ✅ تسجيل جميع العمليات الإدارية

## 🐛 استكشاف الأخطاء

### البوت لا يستجيب
- تأكد من صحة `BOT_TOKEN` في ملف `.env`
- تحقق من اتصال الإنترنت
- راجع سجلات الأخطاء في الكونسول

### التحميل لا يعمل
- تأكد من صحة الرابط
- تحقق من دعم المنصة
- تأكد من وجود مساحة كافية في `TEMP_DIR`

### قاعدة البيانات لا تعمل
- تأكد من وجود مجلد `data`
- تحقق من صلاحيات الكتابة
- راجع `DB_PATH` في ملف `.env`

## 📝 الترخيص

MIT License - يمكنك استخدام وتعديل البوت بحرية.

## 🤝 المساهمة

المساهمات مرحب بها! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع جديد للميزة
3. إرسال Pull Request

## 📞 الدعم

للدعم والاستفسارات:
- افتح Issue في GitHub
- راسلنا على Telegram

## 🎯 الميزات القادمة

- [ ] دعم المزيد من المنصات
- [ ] تحميل قوائم التشغيل
- [ ] تحويل الصيغ
- [ ] ضغط الملفات
- [ ] إحصائيات متقدمة
- [ ] نظام النقاط والمكافآت
- [ ] API للمطورين

---

صُنع بـ ❤️ للمجتمع العربي
